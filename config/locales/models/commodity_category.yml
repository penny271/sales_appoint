ja:
  activerecord:
    models: # モデルの属性名
      commodity_category: 商材カテゴリ
    attributes:
      commodity_category:
        name: 商材名
    errors:
      messages:
        record_invalid: 'バリデーションに失敗しました: %{errors}'
        restrict_dependent_destroy:
          has_one: "%{record}が存在しているので削除できません"
          has_many: "%{record}が存在しているので削除できません"
  commodity_categories: # マニュアルでテキストを登録する場合の書き方
    form:
      add_item: "登録"